const targetAll = 'all';
const targetDev = 'dev';
const targetEmu = 'emu';

const constants = {
  adb: 'adb',
  availableCommands: [targetAll, targetDev, targetEmu],
  targetAll,
  targetDev,
  targetEmu,
};

export default constants;
